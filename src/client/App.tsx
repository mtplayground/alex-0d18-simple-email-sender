export function App() {
  return (
    <main className="page-shell">
      <section className="intro" aria-labelledby="page-title">
        <p className="eyebrow">One-page email sender</p>
        <h1 id="page-title">Send an email from a simple web form.</h1>
        <p className="summary">
          The project is initialized with a browser app, server entrypoint, and
          shared email types so each follow-up issue can add one focused layer.
        </p>
      </section>
    </main>
  );
}
